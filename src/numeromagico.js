import leia from 'readline-sync'

var numeromagico = math.floor(math.random() * 1000) + 1;

var tentativa;
var qtdtentativas + 0;

console.log("=======jogo numeromagico======")

do {
  tentativa = leia.questioninf("digite sua tentativa: ");
}while(tentativa !== numeromagico);